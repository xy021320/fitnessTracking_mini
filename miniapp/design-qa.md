# 微信小程序交付检查

## 自动检查

- AppID：`wxcbad07e3fb2b6b8e`，根目录与编译产物一致。
- 页面：首页、训练、数据、我的四页均已生成 WXML、WXSS 和 JS。
- 导航：`app.json` 包含四个原生 TabBar 项。
- 训练页：包含添加项目和完成训练，不包含已删除的底部休息计时条。
- 数据：训练量、距离、时长、配速和项目表现均从 session 历史派生。
- 持久化：使用 `zhu-li-app-state-v1` 微信本地存储键，异常时回退初始状态。
- 自动测试：配置、领域规则、Reducer、存储、页面契约和构建产物共 10 项。

## 构建检查

- Taro：4.2.0。
- 构建目标：微信小程序 `weapp`。
- 生产构建：通过。
- npm 安全审计：0 个漏洞。

## 开发者工具人工检查清单

- iPhone 与 Android 模拟器各检查一次四个页面，无横向滚动和底部遮挡。
- 分别创建重量 + 次数 + 组数、次数 + 时长、距离 + 时长项目。
- 完成一条新训练，确认数据页立即更新。
- 重新编译或关闭再打开小程序，确认本地数据仍存在。
- 点击“预览”并使用有权限的微信扫码真机检查。
